{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Tests: Pytest",
            "type": "shell",
            "command": "python -m pytest",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "test"
        }
    ]
}
